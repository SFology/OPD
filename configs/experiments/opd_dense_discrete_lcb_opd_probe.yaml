extends: opd_dense_discrete_lcb_base.yaml

experiment:
  name: opd_dense_discrete_lcb_opd_probe
  description: One-step OPD control probe while measuring exact-action LCB diagnostics.
  tags: [opd, lcb-opd, dense-discrete, control, probe, exact-action, rtx6000-ada, auto-gpu]

distillation:
  robust_opd:
    apply_to_training: false

trainer:
  total_training_steps: 1
  save_freq: 1
  max_actor_ckpt_to_keep: 1
