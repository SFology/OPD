extends: opd_dense_discrete_lcb_base.yaml

experiment:
  name: opd_dense_discrete_lcb_treatment_probe
  description: One-step LCB-OPD treatment probe with exact sampled-action neighborhood scoring.
  tags: [opd, lcb-opd, dense-discrete, treatment, probe, exact-action, rtx6000-ada, auto-gpu]

distillation:
  robust_opd:
    apply_to_training: true

trainer:
  total_training_steps: 1
  save_freq: 1
  max_actor_ckpt_to_keep: 1
