extends: opd_dense_discrete_base.yaml

experiment:
  name: opd_dense_discrete_opd
  description: Full one-epoch OPD control with dense-discrete ROPD measured but not applied.
  tags: [opd, ropd, dense-discrete, control, full, rtx6000-ada, 4gpu]

distillation:
  robust_opd:
    apply_to_training: false
