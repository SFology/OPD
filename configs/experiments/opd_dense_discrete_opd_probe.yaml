extends: opd_dense_discrete_base.yaml

experiment:
  name: opd_dense_discrete_opd_probe
  description: One-step OPD control probe while measuring dense-discrete ROPD diagnostics.
  tags: [opd, ropd, dense-discrete, control, probe, rtx6000-ada, 4gpu]

distillation:
  robust_opd:
    apply_to_training: false

trainer:
  total_training_steps: 1
  save_freq: 1
  max_actor_ckpt_to_keep: 1
