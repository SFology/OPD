extends: opd_dense_discrete_base.yaml

experiment:
  name: opd_dense_discrete_ropd
  description: Full one-epoch ROPD treatment using dense discrete cross-rollout trajectory neighborhoods.
  tags: [opd, ropd, dense-discrete, treatment, full, rtx6000-ada, 4gpu]

distillation:
  robust_opd:
    apply_to_training: true
