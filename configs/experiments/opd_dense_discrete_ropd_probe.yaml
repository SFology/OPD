extends: opd_dense_discrete_base.yaml

experiment:
  name: opd_dense_discrete_ropd_probe
  description: One-step ROPD probe using dense discrete cross-rollout trajectory neighborhoods.
  tags: [opd, ropd, dense-discrete, treatment, probe, rtx6000-ada, 4gpu]

distillation:
  robust_opd:
    apply_to_training: true

trainer:
  total_training_steps: 1
  save_freq: 1
  max_actor_ckpt_to_keep: 1
