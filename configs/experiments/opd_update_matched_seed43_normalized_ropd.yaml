extends: opd_update_matched_seed43_base.yaml

experiment:
  name: opd_update_matched_seed43_normalized_ropd
  description: LCB ROPD with per-batch token-reward RMS restored to original OPD.
  tags: [ropd, normalized-ropd, update-matched, seed43, treatment]

distillation:
  robust_opd:
    training_reward_mode: normalized_ropd
    apply_to_training: true
    normalization_epsilon: 1.0e-8
    max_normalization_scale: 20.0
