extends: opd_update_matched_seed43_base.yaml

experiment:
  name: opd_update_matched_seed43_scaled_opd
  description: Globally scaled OPD control using a frozen, label-free FP32 RMS calibration.
  tags: [opd, scaled-opd, update-matched, seed43, control]

distillation:
  robust_opd:
    training_reward_mode: scaled_opd
    apply_to_training: true
    # Supplied by launch_update_matched_comparison_tmux.sh after calibration.
    fixed_opd_scale: null
